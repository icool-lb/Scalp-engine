module.exports=async function(req,res){try{const symbol=String(req.query.symbol||'BTCUSDT').toUpperCase();const limit=Math.max(1,Math.min(parseInt(req.query.limit||'500',10),1000));const r=await fetch(`https://api.binance.com/api/v3/aggTrades?symbol=${encodeURIComponent(symbol)}&limit=${limit}`,{headers:{Accept:'application/json'}});const j=await r.json();if(!r.ok)return res.status(r.status).json({ok:false,error:j.msg||'Binance aggTrades failed',detail:j});let buyAggVolume=0,sellAggVolume=0;for(const t of j){const q=Number(t.q||0);if(t.m) sellAggVolume+=q; else buyAggVolume+=q}res.setHeader('Cache-Control','no-store,max-age=0');return res.status(200).json({ok:true,source:'binance-aggtrades',symbol,buyAggVolume,sellAggVolume,count:j.length,time:new Date().toISOString()})}catch(e){return res.status(500).json({ok:false,error:e.message})}};